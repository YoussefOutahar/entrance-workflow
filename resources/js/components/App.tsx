import React from 'react';

interface Props {}

const App: React.FC<Props> = () => {
    return (
        <div>
            <h1>Hello TypeScript + React + Laravel!</h1>
        </div>
    );
}

export default App;